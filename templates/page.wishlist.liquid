<script>
  // Fetch the users wishlist - please use it inside the swymCallBackFn
  swat.fetch(function(usersWishlist) {
    // Prints the current users wishlist - 
    console.log("Successfully Fetched the Current users wishlist", usersWishlist);
  }, function(error) {
    console.log("There was an error while fetching the wishlist", error)
  });
</script>